#include"crayon.h"
using namespace std;
using namespace ca;
int main(){
	dataname="奖学金 test";	//数据输出的前缀 
	makein(20){
		csh;	//初始化程序 
		int a=cyrand(10,300);
		inint(a);
		instring("\n");
		for(int i=0;i<a;i++){
			inint(cyrand(0,100));
			instring(" ");
			inint(cyrand(0,100));
			instring(" ");
			inint(cyrand(0,100));
			instring("\n");
		}
	}
	makeout(20);
}
