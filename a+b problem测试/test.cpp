//a+b problem test
#include"caryon.h"
using namespace std;
using namespace ca;
int main(){
	dataname="";
	makein(1,10)/*生成10组in数据*/{
		csh;
		inint(cyrand(0,100));
		instring("  ");
		inint(cyrand(0,100));
	}
	makeout(1,10);
	closefile();
}
