#include"caryon.h"
using namespace std;
using namespace ca;

int main(){
    makein(1,10){
        csh();
        int m;
        int n;
        m=cyrand(1,20);
        n=cyrand(1,20);
        inint(n);
        instring(" ");
        inint(m);
        instring(" ");
        inint(cyrand(0,20));
        instring(" ");
        inint(cyrand(0,20));
    }
    makeout(1,10);
    return 0;
}
